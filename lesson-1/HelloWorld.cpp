#include <stdio.h> 
/**
 * - Tells the preprocessor/precompiler to include the system header stdio.h
 * - stdio.h contains many standard input and output functions, like printf.
 * - The < and > indicate that these are system headings, not customize headings.
 */

int main(void)
{
    printf("Hello world! \n");
    return 1;
}